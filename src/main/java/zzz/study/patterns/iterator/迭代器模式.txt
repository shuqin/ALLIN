/*
 * 迭代器模式：
 * 为顺序访问集合对象提供服务。
 * 
 * 有两种类型的迭代器实现：
 * 1. 外部迭代器： 返回一个迭代器，由用户调用迭代器的公共方法来实现用户所期望的遍历。
 *                比如示例中的 Iterator 接口实现，分别实现了前向遍历迭代器和后向遍历迭代器，
 *                并且实现为内部类，以封装遍历的实现细节；
 *
 * 2. 内部迭代器： 提供迭代的公共方法，该迭代的方式是已经规定好并且封装好的，用户无法改变。
 *                比如示例中的 traverse 方法，提供了访问数组对象的默认方式。
 *
 *
 *
 *
 *
 */