/*
 * 原型模式：
 * 根据现有对象样本来复制生成新对象。
 *
 * 通过复制创建的新对象一般会含有原始对象的某些状态（保留某些属性值），
 * 当几个对象的类仅在属性上存在一点差别，而在行为上完全相同时，
 * 可以通过为用户提供一个可以复制的对象原型，从而在运行时动态创建新类。
 *
 * 原型模式有两种方式：
 *  第一种，先实例化一个新对象，然后将原型对象中所需要的字段赋给新对象的相应字段；
 *  第二种，重写clone方法，注意要实现Cloneable接口。 
 *
 */
 